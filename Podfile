# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SampleApp_SwiftUI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 5.2'
  pod 'ObjectMapper', '~> 3.5'
  pod 'SwiftyJSON', '>= 2.1.3' 
  pod 'RealmSwift'

  # Pods for SampleApp_SwiftUI

  target 'SampleApp_SwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SampleApp_SwiftUIUITests' do
    # Pods for testing
  end

end
